# ============================================================
# LOCAL DEVELOPMENT (defaults — works out of the box, no auth needed)
# ============================================================
LOCAL_DEV=true
OPENAI_API_KEY=sk-...
DATABASE_URL=sqlite+aiosqlite:///./ernesto.db
CORS_ORIGINS=http://localhost:5173,http://localhost:8081

# ============================================================
# AUTH — Supabase (production only, ignored when LOCAL_DEV=true)
# ============================================================
# Create a free project at https://supabase.com
# Then find these values in: Settings → API

# Required: your Supabase project URL (Settings → API → Project URL)
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Legacy HS256 secret — only needed for older projects still on the legacy JWT system.
# New projects use asymmetric keys (ES256/RS256) via JWKS — leave this empty.
# If you see "JWT Secret" in Settings → JWT, paste it here.
SUPABASE_JWT_SECRET=

# Anon / Publishable key — used by the frontend and mobile app, NOT the backend.
# Find it in Settings → API → Publishable key (or Legacy API Keys → anon)
SUPABASE_ANON_KEY=

# ============================================================
# STORAGE — S3 (production only, falls back to ./uploads/ if empty)
# ============================================================
S3_BUCKET=
CLOUDFRONT_DOMAIN=
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# ============================================================
# REAL-TIME — Redis (production only, falls back to in-memory if empty)
# ============================================================
REDIS_URL=

# ============================================================
# DATABASE — PostgreSQL (production only, falls back to SQLite if empty)
# ============================================================
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/ernesto

# ============================================================
# ENCRYPTION — for platform credentials at rest
# ============================================================
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# ============================================================
# eBay API (https://developer.ebay.com)
# ============================================================
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_USER_TOKEN=
EBAY_SANDBOX=true

# ============================================================
# Telegram notifications (optional)
# ============================================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============================================================
# App
# ============================================================
SECRET_KEY=change-me-in-production
